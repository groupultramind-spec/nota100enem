services:
  - type: web
    name: admin-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m http.server $PORT & python admin_bot.py
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
