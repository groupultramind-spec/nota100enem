[build]
  publish = "."
  functions = "netlify/functions"

[[redirects]]
  from = "/api-proxy/*"
  to = "https://api.syncpay.com.br/:splat"
  status = 200
  force = true
  headers = {Access-Control-Allow-Origin = "*"}
